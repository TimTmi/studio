rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access to all documents for any signed-in user.
    // This is insecure for a production app but useful for development.
    match /{document=**} {
      allow read, write;
    }
  }
}
